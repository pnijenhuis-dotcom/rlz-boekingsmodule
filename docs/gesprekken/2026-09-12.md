# Gespreksverslag 2026-09-12 — RLZ-boekingsmodule (Claude Code-sessie, run 2 Vastgoedgroep Nederland → Odoo)

> Conform Platform-besluit 0024. Besluiten/opdrachten letterlijk waar de tekst beschikbaar is; overige onderwerpen samengevat.
> Canonieke vastlegging: `docs/BESLISSINGEN.md` secties "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 (12-09-2026) …" en "RUN 2 — BLOK 1…6".

## Feiten
- Deelnemers: Peter Nijenhuis (opdrachtgever), Claude Code (coördinator + agenten A schoonlijst/snede 2, B pandenregister,
  C RJ-mapping company 6, D Odoo-schrijfpad/company-pin, E replay-motor). Repo `Rlz boekings module`, branch main.
- Productie bij sessiestart: service én 15 jobs op image `5b6270c` (= HEAD vóór deze run), migratiestand 0136. Dev ná deze run:
  **0138** (0137 pand_boeking-soorten RJ 220, 0138 odoo_koppeling RJ-220-rollen + `migratie_doel`).
- Odoo: `universal-steigers.odoo.com`, company 6 "Vastgoedgroep Nederland B.V." is leeg (0 moves, 356 rekeningen, dagboeken F 48 /
  LF 49 / MEM 50 / BNK1 53 zonder IBAN, plan Project 1). Lees-only bereikbaar vanuit dev met de bestaande key (`lees_dev_env`).
- RLZ-VGG: administratie `cc07e461…`, RLZ-admin-id `f6d4770f-…`; data uitsluitend in productie (credential-store) → alle
  RLZ-lezingen via `scripts/gcp/nameting.sh` (impersonatie nameting@; gcloud-sessie info@vastly.software werkte vandaag).
- Geen Odoo-testdatabase (besluit 1); geen Odoo-writes in deze run; geen productie-writes vóór de deploy.

## Opdracht Peter 12-09 (letterlijk, kern)
> "GEEN Odoo-testdatabase. Migratie gaat direct naar de live database, company 6 (leeg), met de bestaande Odoo-koppeling/API-key
> (één gebruiker, tien companies). […] (a) eerst volledige dry-run zonder Odoo-writes mét reconciliatierapport; (b) alles landt als
> CONCEPT in company 6, saldibalans-toets op de concepten, posten in één aparte stap (run 3); fout = annuleren/tegenboeken, nooit
> unlink; (c) harde company-pin […]; (d) bank in twee fasen […]."
> "RJ 220 aangescherpt: aanbetalingen aan verkopers […] = VOORUITBETAALD OP HANDELSVOORRAAD (balans), bij levering in de kostprijs
> van het pand. 'Vaste lasten (volgens afspraak)' die VGG voor de verkoper betaalt = W&V op factuur-/mutatiedatum […]. Vier rollen
> dus: voorraad panden, vooruitbetaald op voorraad, opbrengst verkoop panden, kostprijs verkochte panden."
> "Pandenlijst: Salesforce levert later de canonieke lijst panden […]. Nu: seam voor een externe pandenlijst (CSV-bestand als eerste
> bron), Salesforce-adapter is run 3+."
> "Tweede dubbel-snede over ALLE administraties (Peter vermoedt Zenvoices × module dubbel geboekt)."
> "NIET in deze run: posten in Odoo, massa-bankaflettering, kantelen via de overstap-wizard, Salesforce-adapter, PWA, pandenschermen
> in de module […]. Twijfel over een besluit hierboven = stoppen en vragen, niet zelf invullen."

**Aanvulling Peter 12-09 (tijdens de run, letterlijk):**
> "bank is leidend bij dubbelen. In blok 1 (schoonlijst) en blok 2 (snede 2): een paar/groep is alleen 'vermoedelijk dubbel' als er
> MINDER bankmutaties (PaymentTransactions, cent-exact bedrag, datum ±3 dagen, zelfde teken) tegenover staan dan boekingen. Evenveel
> bankmutaties als boekingen = echt, niet melden (wel tellen als 'bank-bevestigd'). Bank-directe boekingen (Receipts/bankdagboeken)
> zijn per definitie bank-bevestigd. Besluit 5: de drie RLZ-paren vervallen als huiswerk van Peter — ze zijn bank-directe boekingen en
> dus echt."

## Verloop (samengevat)
- Blok 0: bevindingen nametingen 11-09 vastgelegd; STAP-0 lees-only op 120 VGG-records (rlz-lezen, 4 collecties): de "spatie op
  positie 32" is een `\n`-regelknip van exact 32 tekens in RLZ's `Description`/`Reference` — feit in api-verkenning, reparatie
  `app/rlz/tekst.py::ontknip` (21 tests) door de coördinator, vóór de agenten startten.
- Vijf agenten parallel op eigen test-databases (`boekhouding_test_a…e`) met bindend contract `CONTRACT_RUN2.md` (scratchpad);
  migratiestubs 0137/0138 vooraf. Uitkomsten per blok: zie BESLISSINGEN "RUN 2 — BLOK n".
- Blok 7 (nameting productie + Odoo-STAP-0 op company 6) kan pas ná de deploy van deze commits → volgende beurt.

## Uitkomst per blok (samengevat; canoniek in BESLISSINGEN)
- **Blok 1+2 (A):** `app/migratie/bankdekking.py` (bank leidend), schoonlijst mét categorieën systeemhulzen / kopie-van-geboekt /
  verschillend kenmerk / bank-bevestigd; snede 2 alleen in de lees-only-run van `rlz_dubbel` (dagelijkse run ongewijzigd). 106 tests.
- **Blok 3 (B):** afleiding herbouwd (ontknip, vulwoorden, notaris-dossierformaat, soorten aanbetaling/vaste_lasten/balans, 31-12 =
  balans, bank-directe boekingen op teken), `pandenlijst.py` (seam + CSV + clustering), `pand_per_document`. 263 tests. Afwijking
  van de opdrachttekst als beslispunt: memoriaal RLZ-06 + "overdracht" + adres blijft AANKOOP (de zeven "Overdracht …"-memorialen
  zijn notaris-afrekeningen ná aanbetalingen); verkoop uit een memoriaal alleen bij expliciete verkoop-woorden; bank-direct volgt het
  teken (de zes Ouwerkerk-ontvangsten = verkoop).
- **Blok 4 (C):** `app/odoo/rj220.py` + CLI `vgg-rekeningen`; lees-only op company 6: voorstel 325000 voorraad panden, 326000
  vooruitbetaald op voorraad, 803100 opbrengst verkoop panden, 701300 kostprijs verkochte panden; "Overhead"-analytic bestaat nog
  niet; type-poort op de grootboekvertaling (1300 Debiteuren → 130000 Creditors via code_verlengd = valse vriend). 31 tests.
- **Blok 5 (D):** company-pin `CompanyGepindeClient`, kill-switch `migratie_odoo_writes_ingeschakeld` (default UIT), primitieven
  `odoo_schrijf.py`, CLI's `odoo-koppeling-migratiedoel` en `vgg-odoo-stap0` (door nameting.sh hard geweigerd). 48 tests. Geen Odoo-write.
- **Blok 6 (E):** `vgg-replay --dry-run` (rlz_bron / vertaling / rapport), anker uuid5 in `ref` als `mig:<anker>`, saldibalans RLZ vs
  berekend Odoo, open posten, per pand, EXPORT juli 2025. 45 tests.
- **Blok 7:** niet uitvoerbaar in deze beurt (deploy volgt op de push) — meetrecepten per blok in BESLISSINGEN.

## Openstaand voor Peter (zie eindrapport en de beslispunten per BESLISSINGEN-sectie)
Rekeningnummers per rol (325000/326000/803100/701300) en CLI vs UI voor het aanmaken; memoriaal-"overdracht" = aankoop (B);
kopie zonder omschrijving (A); snede 2 als nachtelijke bevinding ja/nee; statements per maand ja/nee; bank-aanlevering ná kanteling
A/B; reconcile tegen een concept-factuur (Odoo eist waarschijnlijk posted → run 3); huiswerk IBAN op BNK1 en Salesforce-toegang.

