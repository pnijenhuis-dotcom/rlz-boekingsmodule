# Gespreksverslag 2026-08-30 — RLZ-boekingsmodule (Claude Code-sessie)

> Eerste verslag in dit project conform Platform-besluit 0024 (gespreksverslagen verplicht in élk project,
> `docs/gesprekken/JJJJ-MM-DD.md`). Besluiten en opdrachten letterlijk; overige onderwerpen samengevat.

## Feiten
- Deelnemers: Peter Nijenhuis (opdrachtgever), Claude Code (bouw). Repo `Rlz boekings module`, branch main.
- Cloud: rlz-boekhouding / europe-west4, Cloud SQL rlz-sql2; migraties landen uitsluitend via de deploy-pipeline
  (migratie-job vóór revisie). gcloud-token op Peters Mac verlopen (interactief `gcloud auth login` nodig).
- Dev-server poort 8000 draaide sinds 28-08 zónder `--reload` (oude code) → herstart mét reload.
- Voorraad-BV's (5) bestaan alleen in de cloud-DB; hernormalisatie daar = klikpunt (zie onder).

## Opdracht 1 (verbatim, Peter) — voorraad-normalisatie v2
> OPDRACHT — voorraad-normalisatie v2 (besluiten Peter 29-08 avond, BESLISSINGEN-capture "OPDRACHT 29-08 blok A"
> + memory; gates staan inmiddels AAN op alle vijf BV's)
> A. Dienst-regels BLIJVEN in de MI-laag. "Uitgesloten" wordt een soort-label (artikel / dienst / transport) […]
> B. Dienst-regex uitbreiden op de bevindingen van 29-08 […] MÉT controlemechanisme (eis Peter, nooit blind vertrouwen) […]
> C. Artikelcode als deterministische normalisatiesleutel MÉT inkoop↔verkoop-match […] Nooit aannemen dat in- en
> verkoopcodes gelijk zijn. Vastgelegd besluit: steigerdelen 3m en 5m zijn verschillende producten, apart tellen.
> D. Ná de bouw: hernormalisatie op de cloud draaien (strikt geen RLZ-writes) en hetzelfde rapport als gisteren […]

Uitkomst: A–C GEBOUWD + GETEST (migratie 0088, commits 7a453c4…bc12ec0; BESLISSINGEN "OPDRACHT 30-08").
D NIET uitgevoerd in deze sessie: cloud-DB nog op 0087 (deploy volgt ná push) én verlopen gcloud-token —
draaiboek in BESLISSINGEN blok D; voertuig `backend/scripts/cloud_cli.py voorraad-hernormaliseer`.
Ontwerpbesluit onderweg (Claude, gemotiveerd): migratie 0088 puur DDL — Alembic op Cloud SQL heeft geen BYPASSRLS,
een data-UPDATE zou stil 0 rijen raken; legacy 'uitgesloten' wordt door de app-hernormalisatie omgezet.

## Opdracht 2 (verbatim, Peter) — instellingen-administraties v2 + terugkerende-facturen-signaal
> BLOK A — Instellingen › Administraties herontwerp (mockup instellingen-administraties-v2.html = bouwnorm,
> akkoord Peter 29-08; beslispunten in de mockup gelden als besloten): compacte tabel […] detail-dialoog […]
> Defaults: boeken_ingeschakeld en ai_extractie_ingeschakeld default AAN voor NIEUWE administraties […]
> Vastly-autoboeken: losse toggle vervalt uit de UI — gedrag volgt is_vastgoed (aan = aan, uit = uit) […]
> Archiveren (🗑): administratie-status gearchiveerd (0052-patroon) — webservice-login intrekken uit de
> credential-store, syncs stoppen, documenten/historie blijven, filter "gearchiveerd (N)", dearchiveren mogelijk
> mits nieuwe webservice-login; registersync levert gearchiveerde rijen niet meer (verdwenen-semantiek §8). NOOIT verwijderen.
> BLOK B — Terugkerende-facturen-signaal (benchmark-besluit Peter 29-08, "inbouwen") […] ≥ 3 facturen met een
> regelmatig interval (maand/kwartaal, tolerantie ±35%) […] Signaal 1 "verwachte factuur ontbreekt" […] Signaal 2
> "prijsstijging": nieuwe factuur > X% (default 10, instelbaar) […] géén AI — puur code.

Uitkomst: GEBOUWD + GETEST (migraties 0089/0090; BESLISSINGEN "OPDRACHT 30-08 (2)"). Contract v1.19 + Platform-
besluit 0025 (gearchiveerde administraties uit de registersync). Afwijking t.o.v. mockup, gemotiveerd: de
bulk-selectie (checkbox) blijft naast de compacte tabel — bestaande feature, niet gevraagd te schrappen.

## Openstaand voor Peter
1. `gcloud auth login` → ná groene deploy `cloud_cli.py voorraad-hernormaliseer` (rapport → BESLISSINGEN).
2. Consequentie deploy: verkoop-autoboeken staat AAN voor álle is_vastgoed-administraties (volgt de koppeling).
3. Vastly informeren over contract v1.19 (geen bouw nodig als afwezigheid al als verdwenen wordt verwerkt).
