# Gespreksverslag 2026-09-08 — RLZ-boekingsmodule (Claude Code-sessies)

> Conform Platform-besluit 0024 (`docs/gesprekken/JJJJ-MM-DD.md`). Besluiten en opdrachten letterlijk waar de letterlijke
> tekst beschikbaar is; overige onderwerpen samengevat. Dit verslag is op 08-09 (avond) aangelegd door de docs-agent van de
> run "app-auth zonder passkey en TOTP"; de eerdere runs van vandaag (bundel 08-09, spoedrun wachtrij, herstelrun "Basis
> eerst", diagnose-run blok 2) zijn hieronder alleen in de Feiten en als verwijzing opgenomen — hun inhoud staat in
> `docs/BESLISSINGEN.md` (secties van 08-09) en in de commits `602660f` … `1c33796`.

## Feiten
- Deelnemers: Peter Nijenhuis (opdrachtgever/eigenaar), Claude Code (coördinator + agenten A backend / B app-frontend /
  C docs / D kantoor-frontend). Repo `Rlz boekings module`, branch main; Platform-repo `../Platform`.
- Productie: Google Cloud `rlz-boekhouding` (europe-west4), Cloud Run `rlz-backend` (revisie ≥ 00462, extractie-trigger
  bewezen), Cloud SQL `rlz-sql2` (CMEK), migratiestand productie 0124 (dev ná deze run: **0125**).
- Native app: bundle-id `nl.aknijenhuis.goedkeuren`; iOS via Xcode Cloud (buildnummer = `CI_BUILD_NUMBER`, eerstvolgende
  build op `main` = **90**; `MARKETING_VERSION` 1.0, repo-`CURRENT_PROJECT_VERSION` 3); Android AAB via
  `native/scripts/bouw_android_release.sh <versionCode> 1.0` — **versionCode 4** (AAB van 08-09 15:36 is van vóór deze run
  en moet opnieuw gebouwd worden; nog niet geüpload). Stores: App Store Connect (PDL Powerhouse; versie 1.0 status
  Rejected 2.1 op build 44 → resubmit via "Update Review" op de versiepagina) en Google Play Console (interne test-track;
  App access = §11). Reviewer-demo-account `p.nijenhuis+applereview@kempengroep.nl` op SEED-PASSKEYTEST (fictieve facturen).
- Auth-stand ná deze run: kantoor-web ongewijzigd (passkeys eerst, wachtwoord + TOTP terugval — 0020); native app +
  accordeur-PWA = toestelbinding + 5-cijferige toegangscode (0029). Sunset legacy-app-auth-routes: **2026-10-08** (tot dan
  `Deprecation`/`Sunset`-headers, daarna 410).
- Andere runs van 08-09 (verwijzing): bundel 08-09 blok 1–8, spoedrun wachtrij 10 s, herstelrun "Basis eerst" (elf
  blokken, migraties 0123–0124, WERKWIJZE v1.14 "Gouden set"), diagnose-run blok 2 (passkey-registratie iOS 07-09 22:01).

## Run avond 08-09 — app-auth zonder passkey en TOTP (besluit Peter 08-09; platformbesluit 0029)

**Besluit Peter (vastgelegd in het bindende bouwcontract van de run, `CONTRACT_APP_AUTH.md` §0):**

> De native accordeur-/veldwerker-app (Capacitor) én de accordeur-PWA (browser, `/accordeur`) kennen nog precies één
> toegangspad: **uitnodiging → activatie op dít toestel (link óf 8-tekens activatiecode) → 5-cijferige toegangscode**.
> Passkey/WebAuthn, TOTP en wachtwoord-login verdwijnen volledig uit de app-oppervlakte. De kantoor-webapp blijft
> ONGEWIJZIGD (passkeys eerst, wachtwoord + TOTP terugval — platformbesluit 0020 blijft daar gelden). Toestel = factor 1
> (langlevend apparaat-token in Keychain/Keystore/IndexedDB), toegangscode = factor 2 (lokaal anker, bereikt de server nooit).
> Blok 3 (PWA) volgt het ADVIES uit de opdracht: zelfde model als native; de coördinator noteert het beslispunt.

> **Peter (opdracht 08-09, letterlijk):** "De native app (Capacitor: klant-accordeurs én veldwerkers — ZZP'er/uitvoerder/detacheerder) kent nog precies één toegangspad: uitnodiging → activatie op dit toestel → 5-cijferige toegangscode. Passkey/WebAuthn, TOTP en wachtwoord-login verdwijnen volledig uit de app. De kantoor-webapp (passkeys eerst, wachtwoord+TOTP als terugval) blijft ONGEWIJZIGD. Aanleiding: passkey-registratie strandt structureel (Peter 08-09, Apple-reviewer 07-09 22:01, Google-reviewer 03-09, iPhone-gebruiker 22:01) op randvoorwaarden die de doelgroep niet kent."
> Blok 3 (PWA): "Advies: zelfde model (activatie via link/code, token in IndexedDB versleuteld met het code-anker, zelfde 5-pogingen-regel); passkey ook daar weg zodat er één verhaal is. Alternatief: PWA houdt passkey. Bouw het advies tenzij Peter anders zegt; noteer het beslispunt."

**Aanleiding (samengevat):** vier gestrande gevallen op de passkey-laag in de app binnen een week — Peter zelf (08-09),
Apple-reviewer 07-09 22:01 (7× registratie-opties zonder voltooien, geen iCloud-sleutelhanger), Google-reviewer 03-09
(18× wachtwoordstap OK, "No create options available" zonder Google-account), iPhone-gebruiker 07-09 22:01 (5× idem).

**Uitgevoerd (samengevat, per blok — status "gebouwd 08-09 — productie-nameting volgt ná deploy", details in BESLISSINGEN
"APP-AUTH ZONDER PASSKEY — TOESTELBINDING + TOEGANGSCODE (besluit Peter 08-09)"):**
1. Backend (agent A): migratie 0125 (activatiecode op de uitnodiging, `soort`/`platform`/`niet_meer_gebruikt_op` op
   `webauthn_credential`, tabel `activatiecode_poging`), `POST /auth/app/activeren` (publiek, 400/409/429, generieke
   meldingen — 0022), `POST /auth/app/toegangscode-gewijzigd`, `X-App-Slot`, sunset-dependency, CLI `app-passkeys-markeren`,
   seed `--genereer-activatiecode`, mail met codeblok.
2. Native app (agent B): nieuw scherm "Activatiecode invoeren" (`AppActiveren.tsx`), vertakking zonder login-/passkey-
   schermen, ⚙ Toegang "Toegangscode wijzigen" + "Dit toestel loskoppelen", passkey-code uit de bundel, guard-test.
3. PWA (agent B): zelfde model, IndexedDB-adapter alleen op `/accordeur`; kantoor-web merkt er niets van (guard).
4. Draaiboeken (agent C): TESTFLIGHT §0e + §1 stap 6, PLAY §10-noot + §11 (nieuwe tekst ≤ 500 tekens), STORE_GEREEDHEID §1.
5. Documentatie (agent C): Platform 0029 + INDEX + 0020-noot + WERKWIJZE v1.15 + OPEN_ITEMS; BESLISSINGEN-sectie;
   CLAUDE.md-auth-alinea ingekort (archief aangevuld); PARKEERPOSTEN P-23/P-24; AVG doc 05/06/07/12 datumnoten;
   WAT_IS_NIEUW-release; dit verslag.
6. Kantoor-frontend (agent D): activatiecode in de uitkomst-banner op /gebruikers, toestel-rijen in de apparatenlijst,
   ActivateScreen flow `"app"`.

**Beslispunten voor Peter:**
1. **Blok 3 — PWA volgt het advies (zelfde model als native, token in IndexedDB versleuteld op het code-anker, passkey ook daar
   weg).** Alternatief: PWA houdt passkey. Terugdraaibaar zonder de native flow te raken.
2. **Bijlage A AVG-akkoordtekst** noemt nog "wachtwoord versleuteld, eventuele passkey" — nieuwe tekstversie (voorgestelde
   bewoording in doc 05) vraagt alle gebruikers opnieuw om akkoord; nu doen of bij de eerstvolgende pakket-herziening?
3. **Sunset-datum 2026-10-08** voor de legacy-app-routes (daarna 410; fysiek verwijderen + `NatievePasskey`-plugin opruimen =
   P-23/P-24). Akkoord op de datum?
4. **Android versionCode:** 4 hergebruiken (AAB van 15:36 nog niet geüpload) of direct 5?
5. **Reviewer-activatiecode** is herbruikbaar en niet-verlopend voor uitsluitend het demo-account — akkoord, of tóch 72 u met
   een verse code per indiening?

**Klikwerk Peter (volgorde, zie BESLISSINGEN-sectie "Klikwerk"):** deploy → seed `--genereer-activatiecode` → CLI
`app-passkeys-markeren` → kliktest build 90 (TestFlight) + AAB vc4 (interne test-track) + PWA op de telefoon → consoles
(ASC Password = activatiecode + Notes §1 stap 6 + Resolution Center + "Update Review"; Play App access §11) → productie-
nameting (audit `toestel_geactiveerd`, geen `registratie/opties`-hits meer) → rapportregel "werkt in productie: ja/nee".
