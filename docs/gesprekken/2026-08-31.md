# Gespreksverslag 2026-08-31 — RLZ-boekingsmodule (Claude Code-sessie)

> Conform Platform-besluit 0024 (`docs/gesprekken/JJJJ-MM-DD.md`). Besluiten en opdrachten
> letterlijk; overige onderwerpen samengevat.

## Feiten
- Deelnemers: Peter Nijenhuis (opdrachtgever), Claude Code (bouw). Repo `Rlz boekings module`, branch main.
- Mockup `mockup/planning-werkopdracht-transport.html` stond klaar in de werkboom en is per de
  opdracht de bouwnorm 1-op-1 (ontwerpnotities onderin = onderdeel van het akkoord); gecommit.

## Opdracht (verbatim, Peter) — planning-uitbreiding + mini-punten
> OPDRACHT — planning-uitbreiding (mockup planning-werkopdracht-transport.html = norm, akkoord Peter 31-08) + mini-punten
> BLOK A — Werkopdrachten per project × periode: nieuwe entiteit werkopdracht (project, van- t/m-datum, vrije tekst;
> meerdere én overlappende per project; APPEND-ONLY mét wie/wanneer, wijzigen = nieuwe versie, historie in de popup);
> dag-override sparse, alleen die dag wint; kantoor: paarse chip + ⊕ per projectrij, popup; veld-app toont de geldende
> opdracht(en) per geplande dag, alleen-lezen. Bewust GEEN push bij wijziging.
> BLOK B — Transport: DAG-AGENDA zonder projectrijen (kaart = projectnr + klantnaam + adres + ▲/▼ + materiaal +
> transporteur + planner + voertuig + status; sleepbaar mét klik-alternatief); werkbakje (zoek → chip → sleep/klik-klik,
> chips blijven staan); statusflow her-enum gereserveerd (rood) → bevestigd (oranje) → definitief (groen) → geleverd
> (grijs), migratie bestaande rijen gepland→gereserveerd; seam zet_transport_status(bron=) blijft; bevestig-popup mét
> VERPLICHTE voertuigkeuze combi/voorwagen + mail aan het TRANSPORT-CONTACT; materiaallijst-popup (volledige catalogus,
> zoekveld, m²-som, transportplanner) → definitief + lijst aan het MATERIAAL-CONTACT; wijzigen ná definitief = delta-mail
> (alleen oud→nieuw, bestel-update-patroon, mailfout = geen stille wijziging); dag verschuiven = terug naar GERESERVEERD
> (materiaallijst blijft); signaalkaart "nog te plannen"; leverancier krijgt transport- én materiaal-contact.
> BLOK C — Rechten: leverancier-/catalogusbeheer verruimd naar Beheerder ÓF B+P; nieuw fijnmazig recht "veldwerkerbeheer"
> (0019-patroon, Beheerder-only toekennen, audit): B+P mét recht mag UITSLUITEND veldwerkers aanmaken (incl.
> uitnodiging_later) en archiveren binnen de eigen scope — nooit kantoorrollen/rol-/scope-mutaties; "+ Project aanmaken"
> terug op /planning voor B+P via de bestaande projectmotor.
> BLOK D — Mini-punten: (1) leesbug /gebruikers module-recht-kolom (casus Haci); (2) administraties-tabel toont de
> werkelijke stand (gedempte default-chips); (3) accordeur centreer-sweep ("Staande goedkeuringen" iPad); (4) veld-app
> uploadvelden eigen knop + ellipsis; (5) koppelcontract §8 versheidsnotitie (casus GB-8003) — Platform-repo.

## Uitkomst
Alles GEBOUWD + GETEST (migratie 0091; BESLISSINGEN "PLANNING-UITBREIDING 31-08" is canoniek):
backend-suite volledig groen, frontend vitest 752 + `tsc -b` groen; migratie-afsluitroutine gedraaid.

Ontwerpbesluiten onderweg (Claude, gemotiveerd — vastgelegd in BESLISSINGEN/migratie-docstring):
1. **Legacy 'gepland' blijft in de CHECK en gedraagt zich overal als 'gereserveerd'**
   (`effectieve_status`): migratie 0091 is puur DDL (0088-les: Alembic op Cloud SQL heeft geen
   BYPASSRLS — een data-UPDATE zou stil 0 rijen raken); een aparte omzet-stap is daardoor niet
   nodig, verschuiven ruimt een legacy-rij en passant op.
2. **Veldwerkerbeheer op een eigen module-sleutel `boekhouding.veldwerkerbeheer`**: de PK van
   `gebruiker_module_rol` is (gebruiker, module) — één rij per module — en Haci moet meerwerk-
   én veldwerkerbeheer-recht tegelijk kunnen dragen. Bijbehorend: additieve verruiming van
   `platform.actor_is_module_beheerder()` naar `boekhouding.%` (vastgoed-tak onaangeroerd) en
   een zelf-gepoorte SECURITY DEFINER `platform.veldwerker_scope_binnen_actor` voor de
   scope-⊆-toets (RLS laat een niet-Beheerder andermans scope buiten de eigen administraties
   niet zien; fail-closed).
3. **Werkopdracht-append-only óók op DB-niveau**: GRANT zonder UPDATE/DELETE (strenger dan het
   0074-patroon) — wijzigen kán alleen als nieuwe versie-rij.
4. **Mail-first op álle drie de transport-mails** (bevestigen/definitief/delta), exact het
   bestelrevisie-patroon: mailfout = 502 + audit, géén status- of lijstwijziging.
5. **Oorzaak mini-punt 1** (leesbug /gebruikers): `module_recht_houders()` las via een
   actor-loze `scoped_session(None)` — de RLS-leespolicy geeft dan stil nul rijen; fix = actor
   verplicht + regressietest die schrijf- en leespad tegen elkaar houdt.
6. **`maak_uitnodiging` zet de administratie-GUC per scope-rij** (RLS-WITH-CHECK op
   gebruiker_administratie voor een niet-Beheerder-actor — nodig voor het veldwerkerbeheer-pad,
   gedrag voor de Beheerder ongewijzigd).

Contract-kant: §8-versheidsnotitie toegevoegd als **v1.19-notitie** (erratum-patroon, geen
versiebump, geen wire-wijziging) in de Platform-repo.

## Openstaand voor Peter
1. Kliktest planning-uitbreiding (Personeel-chip/popup/dag-override, Transport-dag-agenda mét
   werkbakje + bevestig-/materiaallijst-popups, veld-app-werkopdracht) — op dev of ná deploy.
2. Leverancier-contactpersonen invullen (Roland/Theo bij Universal Verhuur e.a.) in
   Instellingen › Materiaalcatalogus — zonder transport-contact weigert bevestigen (leesbare 422).
3. Veldwerkerbeheer-recht toekennen aan de beoogde B+P-medewerker(s) (/gebruikers, nieuwe
   switch-kolom "Veldwerkerbeheer"); het meerwerk-recht-kolomprobleem (casus Haci) is gefikst.
4. Vastly hoeft níéts te doen voor de §8-notitie (pure documentatie), wel handig om te melden.

---

# Tweede sessie 31-08 — BUGFIX-OPDRACHT: AI-extractie plat (union-limiet, URGENT)

## Opdracht (verbatim, Peter)
> BUGFIX-OPDRACHT — AI-extractie plat: schema boven Anthropic's union-limiet (URGENT)
> Sinds de voorraad-v2-deploy (30-08) faalt élke intake-extractie met: Claude API-fout 400 —
> "Schemas contains too many parameters with union types (19 parameters …) (limit: 16 …)."
> Oorzaak: het inkoop-regelschema is gegroeid (o.a. eenheid `e`, stuksprijs `p`, artikelcode `a`
> voor de voorraadlaag) tot 19 nullable/union-velden; Anthropic's structured-output-limiet is 16.
> Fix: (1) schema onder de limiet — voorkeursrichting sentinel (lege string/0) + deterministische
> normalisatie naar None, géén verlies van velden; (2) regressietest die het aantal union-/
> nullable-parameters in het live-schema telt en faalt boven de 16 + sentinel-normalisatie-test;
> (3) de andere AI-schema's onder dezelfde testpoort; (4) ná de fix de sinds 30-08 gefaalde
> extracties opnieuw aanbieden (bestaande "opnieuw"-route/extractie-wachtrij) — geen nieuwe motor;
> meld het aantal. Bijvangst: scripts/cloud_cli.py-docstring (DOCUMENT_GCS_BUCKET + verse ADC,
> bewezen accordering-herstel-run 31/32). Afsluiting: volledige suites, BESLISSINGEN,
> gespreksverslag, logische commits.

## Uitkomst
Alles uitgevoerd (geen migratie, geen frontend; BESLISSINGEN "BUGFIX 31-08" is canoniek):
- **Fix**: inkoopschema (kop 12 + regel 7 = 19 unions) volledig sentinel-gebaseerd — verplichte
  strings, `""` = onbekend; de bestaande `_als_tekst`-normalisatie (strip, leeg → None) dekte de
  sentinel al, dus intern verandert er niets. FACTUUR-/KOP-/REGELS_SCHEMA = 0 unions.
- **Testpoort** `tests/extractie/test_schema_unionlimiet.py`: teller (anyOf/oneOf + meervoudige
  type-arrays, genest + array-items, zelftest), álle vijf live schema's ≤ 16 (kassarapport 9,
  contract 5, splitsing 3, voorraad-normalisatie 2 — blijven bewust union-gebaseerd), fail-closed
  sweep op nieuwe `json_schema=`-aanroepers; sentinel-test in test_service.py.
- **Nazorg-CLI** `extractie-heraanbieden --sinds --filter --dry-run`: dunne lus over de bestaande
  `herextraheer_document`-route (jongste uitkomst = fout; al-herstelde documenten met rust);
  bijvangst: groot-document-wachtrijdetail draagt nu een `reden` (⚙-vangnet).
- **Cloud-run uitgevoerd**: dry-run 5 kandidaten (alle 5 de union-fout, geen andere gefaalde
  sinds 30-08) → echte run **5/5 heraangeboden** (synchroon, 0 wachtrij, 0 overgeslagen) →
  verificatie-dry-run 0 kandidaten: alle 5 dragen weer een veldvoorstel. Extractie draaide lokaal
  op de gefixte code (cloud_cli); **de cloud-service krijgt de fix bij de deploy van deze commit —
  tot die live is faalt een níéuwe cloud-intake-extractie nog**.
- **Bijvangst**: cloud_cli.py-docstring aangevuld (DOCUMENT_GCS_BUCKET=rlz-boekhouding-documenten
  + verse ADC voor bestand-rakende commando's; ADC-terugval dekt alleen KMS, niet storage).
- Volledige backend-suite groen: 2556 passed, 1 skipped.

## Openstaand voor Peter
1. Niets klikbaars: de deploy van deze commit maakt de cloud-keten weer heel; daarna landt
   nieuwe intake gewoon weer mét AI-voorstel.

---

# Derde sessie 31-08 — verzamelrun avond: btw-crediteur-vangrail, B+P-catalogus-ingang, §8-contractwerk, incognito-passkey-verkenning

## Opdracht (verbatim, Peter)
> OPDRACHT — verzamelrun 31-08 avond: btw-crediteur-vangrail, B+P-catalogus-ingang,
> §8-contractwerk (versheid + inbox_adres), incognito-passkey-verkenning
> BLOK A — Btw-tarief-weigering RLZ (casus Labo Derva 31-08): foutvertaling van RLZ's 400
> "ongeldig belastingtarief" naar leesbare melding mét handelingsperspectief (controlescherm,
> boek_fout-reden, accordering-herstel-CLI); pre-check (waarschuwing, geen blokkade) bij een
> EU-/Ex-EU-tarief terwijl de vendor-cache geen land/btw-nummer kent; api-verkenning-notitie.
> BLOK B — Materiaalcatalogus-ingang voor B+P (casus Haci): kaart + sectie tonen voor B+P
> (alleen deze sectie), fail-closed test op de rol×sectie-matrix.
> BLOK C — Koppelcontract §8: versheidsnotitie (casus GB-8003); optioneel additief veld
> inbox_adres per administratie-rij (verzoek Vastly, akkoord Peter — centrale adres op élke
> actieve rij, bron één config-setting, geen versiebump, wél contract-notitie + test); ná
> deploy meldtekst voor Vastly.
> BLOK D — Verkenning (geen bouw zonder bevinding): incognito-passkey — waarom terugval op
> wachtwoord+TOTP, en kan server-side allowCredentials de passkey dáár ook aanbieden zonder de
> apparaat-gebonden kill-switch-lijn te breken? Alleen beslisnotitie + evt. parkeerpost.
> Afsluiting: volledige suites, BESLISSINGEN-secties, CLAUDE.md waar geraakt, gespreksverslag,
> logische commits (Platform-commit apart voor het contract).

## Uitkomst (BESLISSINGEN "VERZAMELRUN 31-08 AVOND" is canoniek; geen migratie)
- **Blok A**: foutvertaling `vertaal_rlz_boekfout` (één vertaalpunt in de boekmotor dekt
  controlescherm + boek_fout + herstel-CLI; rauwe RLZ-tekst reist mee) + oranje signaalrij
  "Btw-tarief buitenland" (naam-prefix ≠ NL, óók in de RLZ-storings-tak, factuur-btw-nummer als
  overtik-hulp) + api-verkenning "EU-tarieven op PurchaseInvoice-Actions".
  **Afwijking van de opdrachtletter, bewust:** de conditie "vendor-cache kent geen
  land/btw-nummer" bleek onbepaalbaar — read-only probe 31-08 (test-administratie + Kempen
  Facilities, Labo Derva zelf): land én btw-nummer zijn via de RLZ-API in géén enkele vorm
  leesbaar (`Vendors/{id}` óók met fields=all; `Addresses.Country` blijft `{}` óók mét
  `$expand=Country` op de zojuist aangevulde kaart) — meesyncen kan dus niet. Het signaal is
  daarom onvoorwaardelijk bij élk buitenland-tarief (zeldzaam, lage ruis).
- **Blok B**: rol×sectie-matrix `zichtbareSecties(rol)` fail-closed; B+P landt op twee kaarten
  (Beveiliging + Materiaalcatalogus), bereikt `/instellingen/materiaal` met scope-administraties
  (/auth/administraties ∩ uren-&-meerwerk-opt-in uit mijn-toegang); alle andere secties vallen
  terug op Beveiliging; matrix-test dekt alle kaarten × rollen incl. onbekende rol.
- **Blok C**: de versheidsnotitie stond al in het contract (v1.19-notitie, planning-run eerder
  vandaag) — geverifieerd, niet gedubbeld. `inbox_adres` gebouwd: sentinel + model_serializer
  (afwezig = écht afwezig, nooit onbedoeld null), centrale adres op élke actieve rij,
  niet-actief/zonder-config = veld weg; contract v1.19-notitie (2) + §8-veldbullet +
  veldenset-regel verduidelijkt (optioneel-additief = notitie mét beider akkoord) +
  schema-versions-notitie. ⚠️ Gevonden gaatje: `INTAKE_POSTVAK_ADRES` stond alleen op de
  IMAP-job — eigen deploy-stap op de sérvice toegevoegd (waarde past niet in de
  `^@^`-gescheiden --set-env-vars: bevat een `@`).
- **Blok D**: beslisnotitie — server-side allowCredentials per account STAAT ER AL (het
  opties-antwoord is contextvrij, geen apparaat-/browserdata-afhankelijkheid); stille terugval =
  échte 409 (wees-credential: geen actieve rij ná kill-switch/afgebroken registratie/ander
  rp_id) óf browser-providergedrag (dat geeft een zichtbare fout, geen stille terugval).
  Kill-switch-lijn ongemoeid door allowCredentials; wat 'm wél zou breken is
  usernameless/discoverable (anonieme challenge — 0040-waarborg, 0006-procedure). Diagnosestap:
  GET /auth/apparaten/kantoor. Parkeerpost: transports-hints in de descriptors.
- Suites: backend volledig groen, vitest volledig groen, `tsc -b` schoon (zie afsluitcommits);
  overflow-sweep overgeslagen — blok B hergebruikt bestaande kaart-markup, geen nieuwe CSS.

## Meldtekst voor Vastly (ná de deploy versturen)
> §8-registersync: het veld `inbox_adres` staat er vanaf nu in — op élke actieve
> administratie-rij, waarde = het centrale intake-adres facturen@ak-nijenhuis.nl (afwezig op
> niet-actieve rijen = geen uitspraak, conform jullie semantiek; geen versiebump,
> contract-notitie v1.19-notitie (2) + regel in registers/schema-versions.md staan erin).

## Openstaand voor Peter
1. Deploy van deze commit (zet ook `INTAKE_POSTVAK_ADRES` op de service) → daarna de meldtekst
   hierboven aan Vastly.
2. Casus incognito-passkey desgewenst diagnosticeren: /gebruikers → apparaten van het account
   (of `GET /auth/apparaten/kantoor`) — geen actieve passkey-rij = de terugval is correct gedrag.

# Vierde sessie 31-08 — pincode-onboarding + app-lock (mockup = norm) én synthetische bewaking + alerting

## Opdracht (verbatim, Peter)
> OPDRACHT — pincode-onboarding + app-lock (mockup = norm) én synthetische bewaking + alerting
> (best-practice-besluit 1, incidenten 30/31-08)
> BLOK A — Pincode-activatie + app-lock native app (mockup/app-lock-pincode.html = bouwnorm, akkoord Peter 31-08;
> ontwerpnotities onderin zijn onderdeel van het akkoord; commit óók de twee nog niet-gecommitte mockups incl. de
> Vastly-referentievariant): 1. Activatie app-rollen herzien: mail-link → 5-cijferige code kiezen → bevestigen →
> Face ID-vraag + voorwaarden-akkoord → klaar. De WACHTWOORDSTAP vervalt voor app-rollen; de passkey-registratie
> gebeurt onder water bij stap 3 (geen eigen scherm). Herziet de atomaire flow van 28-08: zelfde link-mechaniek en
> atomiciteit (mislukt = niets half, link blijft 72 u), alleen de wachtwoord-parkering vervangt door de
> code-parkering. Zwakke reeksen (12345, 00000, 11111 …) geweigerd met vriendelijke tekst. Voorwaarden-akkoord
> blijft server-side afgedwongen. 2. App-lock: Face ID (Android: BiometricPrompt) is het standaardpad bij openen,
> de code de terugval — de code is het ANKER en ontgrendelt lokaal een TOESTEL-GEBONDEN sleutel die het bestaande
> apparaat-token beschermt. HARDE EIS: iOS-Keychain zónder biometryCurrentSet, Android-Keystore mét
> setInvalidatedByBiometricEnrollment(false) — biometrie-falen of een nieuw ingeschreven gezicht mag nooit
> uitsluiten. De pincode gaat nooit naar de server (geen server-side pinopslag); kill-switch per apparaat blijft
> onverkort bijten. Het lokale slot vervangt de 24-uurs-passkey-assertion bij het openen (server-side
> sliding-refresh blijft ongewijzigd); passkey-assertion blijft voor her-login. 3. 5 foute codes = sessie + code
> lokaal gewist + audit-event, herstel = verse kantoor-link (bestaand poortwachter-model). Instellingen-scherm
> conform mockup: Face ID-switch, code wijzigen (huidige code vereist), direct vergrendelen (anders 5 min),
> toestel ontkoppelen. 4. Scope: native app (iOS + Android). De PWA/web houdt de bestaande passkey-flow.
> Geofence-branch NIET meenemen (blijft apart, store-motivering v1.1). Nieuwe native module → volgende
> store-build voorbereiden (buildnummer bump, TestFlight/interne test; release = klikwerk Peter).
> BLOK B — Synthetische bewaking + alerting (aanleiding: twee stille productie-incidenten dit weekend —
> AI-extractie ruim een dag plat op de schema-limiet, Anthropic-tegoed leeg — beide toevallig ontdekt):
> 1. Probe-job (Cloud Scheduler + Cloud Run job, elk kwartier): health-endpoint, DB-verbinding,
> documentopslag-leesproef, mailkanaal-config; plus 1× per uur een minimale echte AI-call (goedkoopste model,
> valt onder de bestaande kostenmeter) die óók de schema-zelftest raakt. Uitkomsten in een eigen statusrij
> (geen RLZ-writes, RLZ-probe alleen een lichte leesroute op de TEST-administratie). 2. Alerting via het
> bestaande SMTP-kanaal naar p.nijenhuis@kempengroep.nl: alert bij 2 opeenvolgende gefaalde probes van hetzelfde
> type (geen ruis bij één hik), duidelijke onderwerpsregel, en een expliciete herstelmelding zodra de probe weer
> groen is. Idempotent per storing (geen mailstorm). 3. Foutpiek-signaal: extractie-foutratio per uur boven een
> drempel (default 50% bij ≥ 3 pogingen) → zelfde alertkanaal. Dit had de schema-bug binnen een uur gemeld i.p.v.
> na een dag. 4. Post-deploy-smoketest in deploy.yml: ná de revisie-switch health + één kritieke gepoorte route +
> de AI-schema-zelftest aanroepen; faalt er iets, dan is de deploy-run luid rood (geen stille kapotte deploys meer).
> Afsluiting: volledige suites, migratie-routine indien geraakt, BESLISSINGEN + CLAUDE.md, gespreksverslag,
> logische commits (native/frontend/backend/infra gescheiden waar zinvol).

## Uitkomst (BESLISSINGEN "PINCODE-ACTIVATIE + APP-LOCK" + "SYNTHETISCHE BEWAKING + ALERTING" zijn canoniek; migratie 0092)
- **Blok A backend:** `POST /auth/uitnodigingen/activatie-zonder-wachtwoord` (linkpoorten →
  passkey_setup-token, legt níéts vast — één interpretatiepunt t.o.v. de opdrachtletter: de
  "code-parkering" is bewust volledig CLIENT-side, want de code mag de server nooit bereiken;
  server-side valt er dus niets te parkeren en de atomiciteit zit onveranderd in de
  registratie-transactie), wachtwoordloze tak in `rond_uitnodiging_af_met_passkey`
  (wachtwoord_hash blijft NULL, audit `zonder_wachtwoord`), her-login zonder wachtwoord
  `/auth/accordeur/passkey-login/*` (0020-lijn, generiek 409), app-lock-endpoints
  `uitgesloten`/`hulp` (ongeauthenticeerd op credential_id, altijd 204, kill-switch +
  audit + kantoormail) en `ontkoppelen` (eigen apparaat-claim). AASA op het app-domein
  draagt nu een `applinks`-blok (backend-route).
- **Blok A frontend/native:** `frontend/src/api/appSlot.ts` (anker + PBKDF2/AES-GCM,
  refresh-token versleuteld in de opslag, foutenteller, biometrie-seam), schermen
  `PincodeKiezen`/`AppSlotScherm`/`ToegangInstellingen` (mockup 1-op-1), slot-gate in
  `AccordeurApp` (vervangt in native de Ontgrendel-cadans; legacy toestel = code-setup bij
  eerstvolgende opening; her-login reset het slot bewust — consistent met scherm 6),
  native activatieflow in `AccordeurActiveren`, passkey-login-tak in `AccordeurLogin`,
  universal-link-vertaling `nativeAppUrl.ts`. Native: `AppSlotPlugin.swift` (Keychain
  `.biometryAny`, expliciet géén biometryCurrentSet) + `AppSlotPlugin.java` (Keystore
  `setInvalidatedByBiometricEnrollment(false)`, BiometricPrompt BIOMETRIC_STRONG),
  registraties, `NSFaceIDUsageDescription`, `USE_BIOMETRIC` + androidx.biometric,
  applinks-entitlement + autoVerify-intent-filter, buildnummers gebumpt (iOS 3, Android
  vc-default 2; MARKETING_VERSION blijft 1.0 — v1.1 blijft voor geofence), STORE_GEREEDHEID
  §1 bijgewerkt. Geofence-branch onaangeroerd.
- **Blok B:** module `app/bewaking/` + migratie 0092 (`bewaking_probe_run`/`bewaking_storing`,
  platform-schema, geen RLS, geen DELETE-grant), probes conform opdracht (RLZ-probe read-only
  op TEST-administratie `8dbfb856-…`, zonder store-credentials zichtbaar overgeslagen),
  storing-statemachine (2-op-rij-alert, idempotent, herstelmelding, mislukte mail = retry
  volgende run), foutpiek-signaal op `document_gebeurtenis`, union-teller runtime verhuisd
  naar `app/extractie/schema_poort.py` (test importeert dáár; bewakingsschema in de
  sweep-dekking), CLI `bewaking-probe` (exit-contract: falende probe = exit 0 + eigen alert;
  exit 1 = bewaking zelf stuk → F3.2-vangnet) + `deploy-smoketest`; deploy.yml: job in de
  F3-lus + eigen config-stap (SMTP/APP_BASIS_URL/test-administratie/ANTHROPIC_API_KEY) +
  eindstap post-deploy-smoketest (health-curl, /auth/gebruikers zonder token moet exact 401,
  one-off rlz-smoketest-job); f3_jobs.sh: scheduler */15, start bewust NIET gepauzeerd.
- **Migratie-routine:** `make migrate` dev `0091 → 0092` (output in de sessie), dump ververst
  (head 0092), live 200 op /health ná de upgrade (reload-uvicorn stond door de migratie-guard
  stil — touch heractiveerde 'm, gepoorte route gaf netjes 401).
- **Suites:** backend 2588 passed + 1 skipped (incl. 12 nieuwe pincode-/app-lock-tests en
  9 bewaking-tests), vitest 771 passed (90 files, incl. appSlot + nativeAppUrl), `tsc -b`
  schoon. Overflow-sweep overgeslagen: alleen accordeur-app-schermen (eigen mobiele CSS,
  geen kantoor-breakpoints geraakt).
- De twee mockups (`app-lock-pincode.html` + Vastly-referentie) zijn meegecommit.

## Openstaand voor Peter
1. Store-build starten (Xcode Cloud vanaf main → TestFlight; Android
   `native/scripts/bouw_android_release.sh` → interne test) en de app-lock-kliktest op toestel:
   activatie via mail-link (universal link!), Face ID-pad, code-terugval, 5×-fout-uitsluiting,
   code wijzigen, toestel ontkoppelen. NB universal links werken pas zodra de nieuwe AASA live
   staat (volgende deploy) én de app-build met het applinks-entitlement geïnstalleerd is.
2. `scripts/gcp/f3_jobs.sh` herdraaien (owner) — maakt de rlz-bewaking-scheduler (*/15) + IAM;
   daarna de deploy (zet job + post-deploy-smoketest live).
3. Optioneel: de TEST-administratie-login (TESTADMIN) in de credential-store zetten, anders
   blijft de rlz-probe zichtbaar op "overgeslagen".
4. Bewust ter kennisname: de uurlijkse AI-probe kost ~verwaarloosbaar (haiku, enkele tokens,
   telt mee in de €100-maandmeter onder bron 'bewaking'); bereikt de maandmeter zijn limiet,
   dan meldt de bewaking dat voortaan óók per mail (dat was incident 2).
