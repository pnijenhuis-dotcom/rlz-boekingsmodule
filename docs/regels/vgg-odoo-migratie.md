# Regels — Vastgoedgroep Nederland → Odoo (run 1 + run 2) en het pandenregister

> **LEESPLICHT:** lees dit bestand volledig vóór élke wijziging, opdracht of advies in dit domein (CLAUDE.md, Peter 17-09).
> Woordelijk verhuisd uit CLAUDE.md op 17-09-2026 (opdracht "CLAUDE.md: volledige regels per domein"); niets samengevat,
> niets weggelaten. Volgorde = de volgorde in CLAUDE.md (chronologisch per onderwerp). Elke alinea draagt zijn eigen datum,
> migratie en BESLISSINGEN-sectie. Nieuwe besluiten komen hier woordelijk bij (capture-at-acceptance) + BESLISSINGEN-rij +
> hooguit één verwijsregel in CLAUDE.md. Code-paden van dit domein: zie `docs/regels/INDEX.md`.

**Wat het is:** Schoonlijst, pandenregister (`pand`/`pand_boeking`, pand = RLZ-project), replay RLZ → Odoo-move-vorm (lees-only), rekeningmapping, 1001-model, RJ-220-rollen op company 6, bewijscyclus `vgg-odoo-stap0`, `vgg-odoo-migratie` (concepten → toets → auto-posten), metingen via de nameting-workflow.

## Regels (woordelijk uit CLAUDE.md, stand 17-09-2026)

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Vastgoedgroep Nederland → Odoo, run 1 (D3/D4, 10-09 + ADDENDUM 10-09 avond; geen code, geen migratie):** GEEN MI-dashboard in de module (besluit Peter 10-09 avond) — de module houdt alleen Register + Toewijzing (mét kolom Behandeling activeren/periodekost/balans/overhead en VERPLICHT veld verwachte verkoopprijs bij aankoop; kolommen in `pand` = run 2) en publiceert een lees-only leesroute (eigen module-rol 0019 + toestelbinding 0029, geen berekende marges); álle marge-/overhead-/break-even-berekening in een aparte Vastgoedgroep-PWA (`mockup/vastgoedgroep-app.html`, ter akkoord bij VGG); boekmodel RJ 220: pand = handelsvoorraad, geactiveerd = koopsom + direct toerekenbare kosten om verkoopklaar te maken, overige kosten W&V wanneer gemaakt, verkoop = opbrengst tegenover kostprijs verkopen (drie rekeningen, nummers run 2 — odoo-verkenning §11.6); referentie-mockups `mockup/pandenregister-cowork.html` (kantoor, alleen tabs Register + Toewijzing = bouwscope) + `vastgoedgroep-app.html`; `pandenregister.html` dashboard-tab doorgestreept als historie, notities ①–⑩ per stuk herwaardeerd + lees-only Odoo-verkenning §11 (statement lines, reconciliatiemodellen in 19 zonder `rule_type`, verkoopfactuur notaris `tax_ids=[]`, bewijscyclus op de TESTdatabase; `ODOO_TEST_URL`/`ODOO_TEST_API_KEY` als secrets) — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 1: MOCKUP PANDENREGISTER + ODOO-VERKENNING".

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Vastgoedgroep Nederland → Odoo, run 1 (bundel 10-09 blok D1/D2; migratie 0130):** lees-only CLI `migratie-schoonlijst` (concepten, dubbelen bedrag+datum+relatie, open bankregels op `OpenAmount`, dubbele IBAN, zonder-relatie-mét-bijlage; verwachtingen Peter als `--verwacht`, nooit hardgecodeerd) + datalaag `pand`/`pand_boeking` met deterministische afleiding `app/panden/afleiding.py` (aankoop = memoriaal RLZ-06 mét adres + notaris-PDF/dossier, verkoop = verkoopfactuur op notaris; meerduidig = nooit invullen) en CLI `pandenregister-afleiden` (default dry-run, `--schrijf` = voorstellen, mens wint; Overhead-project alleen gerapporteerd — aanmaken = RLZ-write, run 2) — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 1: SCHOONLIJST + PANDENREGISTER-DATALAAG"

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Vastgoedgroep Nederland → Odoo, run 2 (12-09; besluiten Peter 12-09: geen Odoo-testdatabase → direct company 6 als CONCEPT mét harde company-pin en kill-switch, RJ 220 vier rollen incl. vooruitbetaald op voorraad, pandenlijst-seam CSV, bank leidend bij dubbelen; migraties 0137–0138; geen schermen, geen posten):** RLZ knipt bank-omschrijvingen op 32 tekens met `\n` → `app/rlz/tekst.py::ontknip` is de enige normalisatie; schoonlijst mét systeemhulzen/kopieën/verschillend kenmerk/bank-bevestigd; lees-only snede 2 over alle administraties; pandenregister herbouwd (soorten aanbetaling/vaste_lasten/balans, verkopen uit notaris-ontvangsten, clustering/pandenlijst); RJ-220-rollen in de koppeling-rij (`app/odoo/rj220.py`, voorstel 325000/326000/803100/701300); Odoo-schrijfpad `app/migratie/odoo_doel.py` + `odoo_schrijf.py` (CLI's `odoo-koppeling-migratiedoel`, `vgg-odoo-stap0`, door nameting.sh geweigerd); replay dry-run `vgg-replay` met reconciliatierapport — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 (12-09-2026)", "SCHOONLIJST VGG HERZIEN", "DUBBEL-SNEDE 2 OVER ALLE ADMINISTRATIES", "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 BLOK 3", "RJ-220-ROLLEN OP ODOO COMPANY 6", "ODOO-SCHRIJFPAD STAP-0 OP COMPANY 6", "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 BLOK 6" + api-verkenning "Regelknip op 32 tekens" + odoo-verkenning §12.

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Vastgoedgroep Nederland → Odoo, run 2 blok 7 (12-09 avond; antwoorden Peter op de vijf beslispunten: rekeningen 325000/326000/803100/701300 AKKOORD via CLI; ÉÉN echt paar POSTEN als bewuste afwijking van "alles concept"; partners als aparte zoek-vóór-create-stap, concept draagt altijd een partner; kopie zonder omschrijving alleen bank-bevestigd anders `concept_kopie_onbeslist`; snede 2 als bevinding blijft open, statements per maand JA, bank ná kanteling B met A als brug; geen migratie):** stap 0–6 in `vgg-odoo-stap0` (partners, posten, IBAN-klikpunt, terugweg cancel → draft + opnieuw reconcilen), gouden-set-guard sluit fixture-datums uit, recepten `scripts/gcp/vgg_blok7_nameting.sh` + `vgg_blok7_odoo_writes.sh`; productie NIET gemeten (gcloud-sessie verlopen) — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 BLOK 7: NAMETING PRODUCTIE + EERSTE ODOO-WRITES".

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Vastgoedgroep Nederland → Odoo, run 2 blok 7b (13-09; besluiten Peter 13-09: VGG niet btw-plichtig → vijfde rol "Btw-afwikkeling historisch", anker facturen `ref` kaal + `invoice_origin`, statements = lopend saldo per IBAN-journal; migratie 0139):** RLZ-webfilter-blokkering (403 + HTML ná ~700 calls) → regels via de collectie-expand `DocumentLineList(…)`, token-bucket + webfilter-backoff in de gedeelde `RlzClient` (`rlz_max_calls_per_seconde`/`rlz_burst_calls`), blokkering = "RLZ-blokkering — meting ongeldig" zonder doorrekenen; verrekening factuur↔creditnota als paar + cent-exacte regelsom; per pand één bron incl. bankmutaties; pandenregister negatief-aan-notaris = aankoop + cluster-kandidaten/huisnummer-signalen; snede 2 periodieke reeksen uitgesloten + KF-testcasus; scripts SERVICE `rlz-backend`, rood rapport = uitkomst (exit 0); productie NIET gemeten — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 BLOK 7b: FIXES UIT DE PRODUCTIENAMETING 13-09 VÓÓR DE EERSTE ODOO-WRITES" + api-verkenning "Webfilter-blokkering bij >N calls" + odoo-verkenning §12.3.

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Vastgoedgroep Nederland → Odoo, run 2 blok 7c (13-09; fixes uit de productienameting 13-09; geen migratie, geen writes):** regelroute = document-vorm `{collectie}/{id}?$expand=DocumentLineList(…)` (kop mét BookDate + regels; `ManualJournals/{id}/Lines` bestaat niet, collectie-expand wordt stil genegeerd — STAP-0 13-09), ≥ 1 document zonder regels = oordeel "ONVOLLEDIG — niet doorrekenen"; `JournalEntry.EventID` = soortcode, koppeling journaalregel ↔ document bestaat niet (volledigheidstoets per DocumentType); pandenmodel op grootboek-regels (7000 = aankoop, opbrengst = verkoop, notaris-ontvangst alleen bank, vaste activa nooit, controle ± aanbetalingen); btw twee bronnen; partner uit de bank-tegenpartij (rest = beslispunt Peter); saldibalans-groepstoets (b); snede 2 dag-batch; `rlz-lezen` recordpad — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 BLOK 7c: MEMORIALEN, PANDENMODEL, BOEKDATUM, BTW-TELLER, SNEDE 2" + api-verkenning "Memoriaalregels + EventID/BookDate — STAP-0 13-09".

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Vastgoedgroep Nederland → Odoo, run 2 blok 7d (14-09; besluiten Peter 14-09: geen dummy-partner — concept geblokkeerd "partner onbekend — toewijzen in Toewijzing"; per-pand "sluit" = GO-eis bij SCHRIJF c; oordeel kent drie standen ROOD / GROEN / GROEN ZONDER DOEL; geen migratie, geen writes):** memoriaalregels UITSLUITEND uit `DebitAmount`/`CreditAmount` (nooit `NetAmount`, nooit de `CreditOrDebit`-code — de 13-09-tekenfout volgde de normale zijde van de rekening: passiva/opbrengst klapten om) + harde balansguard "memoriaal uit balans" = ROOD; volledigheidstoets per DocumentType op `EventID` (`rlz_bron.DOCUMENT_EVENTIDS`, onbekend = "toets niet uitvoerbaar" mét codes); DocumentType 0 = RLZ-resultaatposten in eigen blok mét sluitcontrole 7999 + 8999 = −0509; betalingsverschil-write-off op RLZ 4900 via de bestaande mapping; `rlz-lezen --record-via-filter` + nameting-stap `e`; **productienameting 14-09 (nazorg): GROEN ZONDER DOEL, werkt in productie: ja — `DOCUMENT_EVENTIDS` 11 → {21}, 19 → {240} (54 = 9 documenten + 45 systeemhulzen, hulzen apart benoemd; 22 = memoriaal zonder document), volledigheidstoets sluit voor álle vier de typen; datumfilters in stap `e` als NL-dag in UTC (`…T23:00:00Z`/`…T22:00:00Z`, een `Z`-literal op middernacht schuift één dag); open modelpunt SCHRIJF b: memoriaal-1001-regels die tegen een statement line reconciliëren → outstanding-/suspense-rekening van het bankdagboek (1001 telt anders dubbel: −85.376,31 / +71.343,31)** — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 BLOK 7d: TEKENFOUT MEMORIALEN, VOLLEDIGHEIDSTOETS, RESULTAATPOSTEN, BETALINGSVERSCHIL" + api-verkenning "Memoriaalregels — teken per regel, STAP-0 14-09".

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Schoonlijst — dubbel = richting + tegenrekening + bankbevestigd (correctie Peter 17-09, casus RLZ-28-00000061/062 = ontvangst + betaling; geen migratie):** groep ± 3 d, profiel uit de REGELS (memoriaal: 10xx-regel bepaalt de richting uit Debit/Credit, tegenrekening mét zijde), ongelijk profiel = "zelfde bedrag, verschillend kenmerk" mét reden, memoriaal mét bankregel = bankbevestiging verplicht, beide bankregels + `bank_toets` op élke rij; RLZ-01-00000006 = Receipt-concept € 43.666,14 13-08-2025 (Rijswijkseweg 409), bankregel "test" = € −1,00 15-08-2026 Koppe open — zie BESLISSINGEN "SCHOONLIJST — DUBBEL = RICHTING + TEGENREKENING + BANKBEVESTIGD (correctie Peter 17-09)".

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Vastgoedgroep Nederland → Odoo, run 2 blok 8 (15-09; "go" Peter 15-09: SCHRIJF a + derde meting; geen migratie):** expliciete rekeningmapping `app/migratie/rekening_mapping.py` — RLZ 1012 Betalingen onderweg → Odoo Outstanding Payments van het bankdagboek (lees-only: payment-method-line → company-default → KLIKPUNT, nooit de bankrekening zelf; in de bankgroep, geen RJ-220-rol), RLZ 1001 als SCHRIJF-b-markering (memoriaal-1001 → outstanding/suspense, bankgroep verwacht rood), voorstel-kolom per ongemapte rekening, replay leest de Odoo-rekeningen zelf via de doelkoppeling, `SCHRIJF a` idempotent; STAP 2–4 UITGEVOERD 15-09 (bron-key = Universal Verkoop, Universal Steigerbouw heeft in productie geen Odoo-koppeling; zes rekeningen 3606–3610 + Overhead 848 op company 6, migratiedoel idempotent; derde meting ROOD alleen op de vier afletter-groepen, 1012 → 135000 id 132 live gevonden; grote job-rapporten gedoseerd printen via `app/migratie/uitvoer.py` omdat Cloud Logging ~500 regels liet vallen) — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 BLOK 8: SCHRIJF a + DERDE METING (Peter 15-09)".

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Vastgoedgroep Nederland → Odoo, run 2 blok 9 (16-09; SCHRIJF b = 1001-model in de replay; geen migratie, geen writes):** memoriaal-1001-regels tegenover een RLZ-bankmutatie (bewijs 1 `PaymentReferenceList`, bewijs 2 cent-exact bedrag + zelfde richting ± 3 d op een vrije mutatie) → outstanding-/suspense-rekening BNK1 (echte id via de 1012-resolutie, anders pseudo-sleutel + KLIKPUNT) en de statement line reconcilieert ertegen; zonder/meerduidig → tussenrekening mét reden; élk resterend groepsverschil in benoemde restcategorieën (afletterstand SCHRIJF c, open mutaties, 1001-rest, RLZ-opruimpunten); rapportsectie "1001-model" (`app/migratie/model_1001.py`); bewijspaar SCHRIJF c = RLZ-01-00000082 (recept); vierde meting NIET gemeten (gcloud verlopen) → inbox-vervolg — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 BLOK 9: SCHRIJF b — 1001-MODEL + VIERDE METING".

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Vastgoedgroep Nederland → Odoo, run 2 blok 10 (17-09; één regel, één bestemming; geen migratie, geen writes):** de RJ-220-rol landde bij een ONTVANGEN aanbetaling (1001 D / 16xx C) op de 1001-regel die het 1001-model óók naar outstanding stuurt (3606 −363.300 / 3607 −20.000 in de vierde meting) → liquide middelen 10xx nooit in de rol-pool, aanbetaling = tegenzijde met het grootste |bedrag|, `rol_regel_index` + overlap-guard = ROOD-bepalend, kolom "RJ-220-tegenzijde → rol" in de 1001-tabel; 24 regels zonder bankmutatie herleid (kruisposten 1011 en spaar↔betaal 1002); vijfde meting ná deploy — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 BLOK 10: ÉÉN REGEL, ÉÉN BESTEMMING (3606/3607)".

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **VGG — concept → auto-posten ná groene toets (Peter 17-09 "liever 1 boeking testen en dan alles definitief"; herziet 12-09 "alles concept"; geen migratie, geen writes):** SCHRIJF c = bewijspaar RLZ-01-00000082 posten + reconcilieren (`vgg-odoo-stap0 --boekstuk`, `VGG_BEWIJSPAAR`), daarna `vgg-odoo-migratie` = concepten → cent-exacte toets uit Odoo (Σ debet = Σ credit, tegenzijde = Σ factuurregels, per pand sluit) → bulk `action_post` in dezelfde run → reconcile; toets rood = niets gepost, concepten blijven staan; kill-switch + `nameting.sh` weigert; GO Peter op het SCHRIJF-c-rapport vóór `SCHRIJF d` — zie BESLISSINGEN "VGG — CONCEPT → AUTO-POSTEN NÁ GROENE TOETS (Peter 17-09)". Vijfde meting 17-09: 3606/3607 Δ 0,00, overlap 0, ROOD alleen afletter-groepen; project-dekking niet gemeten (Project ontbrak in de document-vorm-expand, gefixt) — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 BLOK 10" alinea "Vijfde meting 17-09".

<!-- uit CLAUDE.md § Domeinbeslissingen -->
- **Vastgoedgroep Nederland → Odoo, run 2 blok 11 (17-09; Peter "in RLZ staat alles als project geboekt"; migratie 0155; geen writes):** STAP-0 lees-only: 83 actieve RLZ-projecten (naam = adres), project op de REGEL van inkoopfacturen (RLZ-04-00000887 → 4200 mét project), bank-directe memorialen (RLZ-28-00000061: 1603/1001) zonder project, `JournalEntryLines`/bankmutaties kennen geen Project; instrument "Project-dekking" in `vgg-replay` (pand-relevante groepen 7000/8xxx/1405/46xx-70xx, meetlat ≥ 90 %, regels zonder project als klikwerk, kruistoets project ↔ adres-cluster); `pand.rlz_project_id` + herkomst `rlz_project`, `pandenregister-afleiden --bron project` (pand = project, soort uit de grootboekregel, adres-clustering = terugval + signaal; default blijft `adres` tot de meting) — zie BESLISSINGEN "VASTGOEDGROEP NEDERLAND → ODOO — RUN 2 BLOK 11: PAND = RLZ-PROJECT".

<!-- nieuw besluit/uitkomst 17-09 avond (capture-at-acceptance; BESLISSINGEN "VGG — CONCEPT → AUTO-POSTEN NÁ GROENE TOETS (Peter 17-09)" alinea "Plan + zesde meting 17-09 avond") -->
- **Vastgoedgroep Nederland → Odoo, run 2 blok 12 (17-09 avond; `plan` ná deploy + zesde meting; geen migratie, geen writes):** de schrijf-CLI's `vgg-odoo-stap0` en `vgg-odoo-migratie` vertalen ALTIJD mét de Odoo-rekeningen van de doelkoppeling (`odoo_migratie_run.replay_met_doelrekeningen`, lezer `rekening_mapping.lees_doelgegevens`, lees-only) en élk schrijfrapport draagt de regel "replay-mapping: Odoo-rekeningen gelezen uit company 6 via de doelkoppeling: N" — een replay zonder lezer is zichtbaar ("élke grootboekregel is ongemapt"), nooit stil; SCHRIJF c is alleen uitvoerbaar als het stap0-rapport het bewijspaar als vertaalbaar toont (RLZ-01-00000082 is dat vandaag NIET: RLZ 8000 "Omzet verkopen" ongemapt in company 6 én geen pand-toewijzing → rol 803100 grijpt niet — beslispunt Peter: Toewijzing pand + soort `verkoop` (advies) en/of expliciete mapping 8000); zesde meting: project-dekking 37,2 % < 90 % → default `pandenregister-afleiden --bron` blijft `adres`; kolom "RJ-220-tegenzijde → rol" leeg = data-correct (rol landt op inkoopfacturen; memorialen mét pand zijn balansboekingen) — zie BESLISSINGEN "VGG — CONCEPT → AUTO-POSTEN NÁ GROENE TOETS (Peter 17-09)" alinea "Plan + zesde meting 17-09 avond", RUN 2 BLOK 11 alinea "Zesde meting 17-09", RUN 2 BLOK 10 alinea "Zesde meting 17-09 — kolom". **Productiestand 17-09 avond ná deploy `8549d3c`: lezer-fix werkt in productie JA (executies `rlz-reconciliatie-m2hst`/`-gblm7`: mapping 361, 224 vertaalbaar, pand-eis 39 meetbaar); SCHRIJF c niet uitgevoerd — bewijspaar blijft niet vertaalbaar tot beslispunt 1 (Toewijzing verkoop / mapping 8000) — zie BESLISSINGEN alinea "Plan ná deploy van de lezer-fix 17-09 avond".**

<!-- nieuw besluit 21-09 (capture-at-acceptance; BESLISSINGEN "VGG — BESLISPUNT 1 BESLIST: TOEWIJZING PAND + SOORT VERKOOP RLZ-01-00000082 (Peter 21-09)") -->
- **VGG — beslispunt 1 beslist: toewijzing pand + soort verkoop RLZ-01-00000082 (Peter 21-09; geen migratie, geen Odoo-writes in deze run):** het bewijspaar RLZ-01-00000082 (Receipt € 400.000,00, 19-03-2026, notaris "O.N."/Ouwerkerk, RLZ-id `8b079e5c…`) krijgt een MENS-toewijzing aan pand Schoffelstraat 29, Purmerend mét soort `verkoop` — het deterministische bewijs is de bankkoppeling TransactionId `00112` (20-03-2026 € 52.142,09 Ouwerkerk Notariaat "Betreft: schoffelstraat 29 te Purmerend, ons dossier: 2026.079950.01"), niet een mapping 8000. Instrument = CLI `pand-toewijzen` (`app/panden/toewijzen_cli.py`), het eerste mens-toewijzingspad van het pandenregister: RLZ uitsluitend GELEZEN (`Receipts?$filter=ReceiptNumber eq '…'&$top=2`, terugval SalesInvoices/PurchaseInvoices; 0 treffers = STOP "niet gevonden", 2 = STOP "meerduidig"), pand-code = de adres-sleutel van de afleiding (`AdresVoorstel.code`, nooit een tweede normalisatie), bestaand pand hergebruikt / nieuw pand herkomst `mens` (status `verkocht` bij verkoop mét verkoopdatum = documentdatum, anders `bevestigd`), `pand_boeking` upsert mét herkomst `mens`, zekerheid `hoog`, `bevestigd_door`/`bevestigd_op`, reden en datum/bedrag uit het RLZ-document; dezelfde stand = "ongewijzigd" (idempotent), hetzelfde document op een ÁNDER pand = STOP exit 2 (nooit stil overschrijven, ook niet over een voorstel); audit `pand_toegewezen_mens` + `pand_boeking_toegewezen_mens` oud→nieuw mét `opdracht`-referentie en bij de systeem-actor `namens`; default dry-run (zelfde stappen, teruggedraaid), `--schrijf` voert uit; `scripts/gcp/nameting.sh` weigert het commando hard. Daarmee geldt `vertaling.pand_telt` (herkomst `mens`) en herclassificeert de 8000-regel naar rol `opbrengst_panden` (803100): het paar wordt vertaalbaar voor SCHRIJF c. Productie uitsluitend op de gedeployde job-image ná deploy: dry-run → `--schrijf` → `vgg_blok7_odoo_writes.sh plan` moet het bewijspaar "vertaalbaar" tonen (anders STOP mét de exacte blokkade; een andere ongemapte ledger is beslispunt 2, geen tweede toewijzing) → `SCHRIJF c` → GO Peter vóór SCHRIJF d — zie BESLISSINGEN "VGG — BESLISPUNT 1 BESLIST: TOEWIJZING PAND + SOORT VERKOOP RLZ-01-00000082 (Peter 21-09)".

<!-- uitkomst 22-09 (capture; BESLISSINGEN "VGG — BESLISPUNT 1 BESLIST: TOEWIJZING PAND + SOORT VERKOOP RLZ-01-00000082 (Peter 21-09)" alinea "Uitgevoerd 22-09") -->
- **VGG — toewijzing uitgevoerd + SCHRIJF c gestrand op de pand-analytic-pseudo-sleutel (22-09; geen migratie; Odoo-writes: 2 partners + 2 concepten op company 6, niets gepost):** de mens-toewijzing van RLZ-01-00000082 aan pand `schoffelstraat-29` (soort `verkoop`) staat in productie (dry-run → `--schrijf` → tweede `--schrijf` "ongewijzigd"; audit `pand_toegewezen_mens` + `pand_boeking_toegewezen_mens`), `plan` toont het bewijspaar vertaalbaar (229 vertaalbare documenten, pand-eis 41 meetbaar). **SCHRIJF c strandde op stap 3:** `action_post` op concept 3370 → Odoo 500 `invalid literal for int(): 'pand:schoffelstraat-29'` — `vertaling.py` schrijft de analytic als pseudo-sleutel `pand:<code>` ("run 3 zoekt/maakt de analytic aan") en dat schrijfpad bestond niet; Odoo valideert de sleutel pas bij posten, dus de dry-run kon het niet tonen. **Regels:** (1) een `analytic_distribution` naar Odoo draagt uitsluitend échte `account.analytic.account`-id's — `odoo_schrijf.PandAnalyticOplosser` vertaalt `pand:<code>` via lookup-vóór-create op (`code` = pand-code, `plan_id` = analytic-plan van de doelkoppeling, company ∈ {6, False}): één actief = hergebruik, méér = `AnalyticMeerduidig`, alleen gearchiveerd = STOP, géén = create `{name: adres, code, plan_id, company_id}` + terug-lezen; audit `odoo_migratie_analytic_aangemaakt`/`_hergebruikt`; (2) `maak_concept_move` weigert élke vals mét pseudo-sleutel (`AnalyticNietOpgelost`) vóór er iets naar Odoo gaat — nooit stil doorlaten; (3) een BESTAAND concept mét pseudo-sleutel in zijn regels (3370) wordt vóór het posten hersteld via `account.move.line write` mét audit `odoo_migratie_regel_analytic_hersteld` oud→nieuw (`herstel_regels`), anders strandt poging 2 op hetzelfde anker opnieuw; (4) de dry-run toont per document de analytic-stand ("→ bestaand N (hergebruik)" | "→ ZOU aanmaken (<adres>, plan P)" | "STOP — <reden>") en de migratie-dry-run telt `pand-analytics`; zonder `analytic_plan_id` in de doelkoppeling is aanmaken een zichtbare blokkade per document; (5) `MoveVoorstel.pand` (code/adres/soort) reist mee uit de vertaling (contract `test_moves_vorm_contract`); (6) een gestrande write-executie is een uitkomst: `vgg_blok7_odoo_writes.sh::execute` houdt de exitcode vast en toont het rapport altijd uit Cloud Logging. **Klikpunt Peter (nu bereikt):** IBAN op dagboek BNK1 in company 6 is leeg → stap 4/5 (statement line + reconcile) worden overgeslagen tot de IBAN in Odoo staat; de per-pand-sluit-eis (7d) is pas daarna toetsbaar. Poging 2 = `opdrachten/inbox/2026-09-22-vgg-schrijf-c-poging-2-na-deploy-pand-analytic.md`; GO Peter op dát rapport vóór SCHRIJF d — zie BESLISSINGEN "VGG — BESLISPUNT 1 BESLIST: TOEWIJZING PAND + SOORT VERKOOP RLZ-01-00000082 (Peter 21-09)" alinea "Uitgevoerd 22-09"; rapport `docs/rapporten/2026-09-22-vgg-toewijzing-schoffelstraat-schrijf-c.md`.
