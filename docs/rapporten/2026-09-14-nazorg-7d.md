# Nazorg 7d + CC-inbox standaard `auto` — 14-09-2026 (eerste echte inbox-opdracht)

Opdracht: `opdrachten/gedaan/2026-09-14-nazorg-7d-en-inbox-auto.md` (Cowork, 14-09). Geen migratie, geen productie-writes.
**Werkt in productie: niet gemeten (geen productiegedrag geraakt)** — de replay/nameting zelf is op 14-09 gemeten (GROEN ZONDER
DOEL, werkt in productie: ja; vastgelegd in BESLISSINGEN blok 7d "PRODUCTIENAMETING 14-09"). De aangepaste volledigheidstoets en
de stap-e-datumfilters draaien pas bij de volgende nameting (dagelijks 05:30 UTC of `rlz meting`).

## Wat is gedaan

| # | Punt | Uitkomst | Waar |
|---|---|---|---|
| 0 | CC-inbox default `auto` | Kopcommentaar `scripts/cc_inbox.sh` uitgebreid (waarom, terugval via `CC_INBOX_PERMISSION_MODE=acceptEdits`, deny-lijst blijft); executable-bit hersteld (Cowork's edit had 755 → 644 gezet); CLAUDE.md § Werkwijze "Werkloop automatisch (14-09)" + BESLISSINGEN "WERKLOOP AUTOMATISCH" rij B3 + aandachtspunt (0) | `scripts/cc_inbox.sh`, `CLAUDE.md`, `docs/BESLISSINGEN.md` |
| 1a | `DOCUMENT_EVENTIDS` | 11 → {21} (228 = 228 memorialen), 19 → {240}; `EVENTID_MEMORIAAL_OVERIG = 22` (214 posten zonder document). Bank-direct: 54 posten = 9 geboekte documenten + 45 systeemhulzen → de toets telt geboekt + hulzen en benoemt de hulzen apart ("sluit (54 documentposten = 9 geboekt + 45 systeemhulzen; …)"). Onbekend type blijft "toets niet uitvoerbaar" mét codes. | `app/migratie/rlz_bron.py`, `app/migratie/replay.py::_volledigheid_per_type` (param `hulzen`) |
| 1b | CLAUDE.md ManualJournals | Op lezen is `CreditOrDebit` 1 = CREDIT, 2 = DEBET (34/34 regels, 6 memorialen); bedragvelden leidend; onze PUT's ongewijzigd (RLZ volgt bij schrijven de bedragvelden — bewezen door de werkende omzet-/waarborg-memorialen). Verwijst naar api-verkenning. | `CLAUDE.md` § Reeleezee API |
| 1c | api-verkenning STAP-0 14-09 | Gevuld met de letterlijke waarden per regel (tabel 6 memorialen), vijf bewezen feiten: CreditOrDebit-codering, Debit/CreditAmount eenduidig, `NetAmount` getekend naar de normale zijde (1602 debet → −1.000, 4106 credit → −624,30), EventID-tabel 21/22/71/72/73/51/53/191/240, datum-valkuil. Ochtend-status "niet uitgevoerd" als historie. | `verkenning/api-verkenning.md` |
| 1d | stap e datumfilters | **Afwijking van de opdracht:** niet "zonder `Z`" maar als NL-dag in UTC (`ge 2025-12-30T23:00:00Z and lt 2025-12-31T23:00:00Z`; CEST-datums `…T22:00:00Z`). Reden: api-verkenning "Filters/paging" bewijst dat een literal zónder `Z` een 400 geeft (DateTimeOffset-format); de verschoven `Z`-vorm is bewezen syntaxis én selecteert exact de lokale dag (bewijs: het `Z`-filter op 31-12 gaf 16 regels, alle van 01-01). Bij de volgende nameting controleren: 1601/1100 op 31-12 → 106 + 122 aanwezig; 1602 op 09-08 → RLZ-60-00000003; 1710/8199/1605/4000 op 30-06 → RLZ-06-00000038. | `scripts/gcp/vgg_blok7_nameting.sh` |
| 1e | BESLISSINGEN blok 7d | Aanvulling "PRODUCTIENAMETING 14-09": GROEN ZONDER DOEL, werkt in productie: ja, alle meetlatten; bevinding `ongemapt:1001` (−85.376,31 / +71.343,31: PaymentTransactions dekken 1001 al exact, memoriaal-1001-poot telt dubbel) + **open modelpunt SCHRIJF b**: memoriaal-1001-regels die tegen een statement line reconciliëren → outstanding-/suspense-rekening van het bankdagboek (geen code nu). | `docs/BESLISSINGEN.md` |
| 1f | Flake `test_staande_voorstel_periodiek.py` | Oorzaak: `_open_rondes_met_volgende_stap` (de gedeelde aan-de-beurt-bron) las de open rondes zonder ORDER BY; het staande-goedkeuring-voorstel kiest "de EERSTE gelijke factuur in lijstvolgorde" uit die ongesorteerde lijst → ná een UPDATE van een ronde-rij (akkoord, herberekening) kan de heap-volgorde van Postgres verschuiven en kreeg soms de derde factuur het voorstel. Fix: `ORDER BY aangeboden_op ASC, id` in de bron — wachtrij, teller, meldingen en voorstel lezen nu dezelfde volgorde. Geen skip. | `app/accordering/service.py` |

## Tests

- `tests/migratie`: 229 passed (was 227; +2 nieuwe tests: EventID 22 als overig + hulzen apart / verschil tegen geboekt + hulzen, onbekend type blijft "niet uitvoerbaar"). Fixture `mini_vgg` kreeg de bewezen codes (11 → 21, 19 → 240) en één huls-journaalpost (bedrag 0, achteraan).
- `tests/accordering/test_staande_voorstel_periodiek.py`: 3 × 9 passed ná de fix (vóór de fix 9 passed in deze sessie — de flake reproduceert niet op commando; de oorzaak is op de code aangetoond: een ORDER BY-loze query waarop volgordegedrag leunt).
- Gouden set `tests/keten`: 108 passed, 1 skipped (pre-existing skip). Frontend `keten_sweep.sh` niet gedraaid: geen frontend-wijziging.
- Guards: `test_claude_md_beslissingen_verwijzingen` (CLAUDE.md 108k tekens), `test_rapporten_index`, `test_nameting_workflow`, `test_keten_guard`, `tests/rlz/test_rlz_lezen_cli`: groen. Ruff check schoon op de geraakte bestanden; `ruff format` alleen op eigen bestanden (`service.py` niet geformatteerd — draagt pre-existing format-drift buiten deze wijziging).

## Keuzes zonder Peter (vastgelegd)

1. Datumfilter-vorm (1d): afwijking van de letterlijke opdracht op grond van een gedocumenteerd API-feit; verificatie bij de volgende nameting.
2. Bank-direct-hulzen: de toets telt `geboekt + hulzen` alleen voor DocumentType 19 (RLZ journaliseert de huls); voor andere typen blijft de vergelijking tegen geboekte documenten.
3. CLAUDE.md ManualJournals: de PUT-vorm van onze eigen motoren is NIET gewijzigd — de werkende productie-memorialen bewijzen dat RLZ bij schrijven de bedragvelden volgt; alleen de leesregel is gecorrigeerd.
4. WAT_IS_NIEUW: leeg gelaten (geen klantzichtbaar gedrag).

## Volgende stap

- Nameting van 15-09 (05:30 UTC) toont de volledigheidstoets voor 11 en 19 als "sluit" en stap e met de juiste dagen — dat is de nameting van dit blok.
- Open modelpunt SCHRIJF b (1001 → suspense) staat in BESLISSINGEN blok 7d; beslispunt Peter vóór de eerste Odoo-write van bankgerelateerde memorialen.
